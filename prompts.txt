הנה הצעות לפרומפטים שונים ומפורטים שתוכלי לשים בקובץ ה-`prompts.txt` של הפרויקט, יחד עם דוגמה לתשובות שתוכלי לקבל מה-AI:

---

### 1. פרומפטים לבדיקת תקינות וארכיטקטורה (Code Review)

#### **פרומפט 1:**

> "אני מצרף קוד לפרויקט Express שנכתב בארכיטקטורת שכבות (Routes, Controllers, Services, Data). אנא סקור את מבנה התיקיות והקוד, והער לי על שגיאות תכנוניות, בעיות בטיפול בשגיאות (Error Handling), או אי-דיוקים בשמות הקבצים והפונקציות."

תשובה
> *"בסקירת הקוד שרשמת, מבנה השכבות תקין ומפריד נכון בין הלוגיקה ל-Data Layer. עם זאת, ב-`coursesController.js` מומלץ לעטוף את הבקשות ב-`try-catch` כדי למנוע קריסה של השרת במקרה של שגיאה בלתי צפויה."*

---

### 2. פרומפטים לבדיקת REST API ונקודות קצה (Endpoints)

#### **פרומפט 2:**

> "כתבתי API ב-Node.js לניהול קורסים ותלמידים. תן לי רשימה של בדיקות Postman שעלי לבצע (לרבות מתודות GET, POST, PUT, DELETE), כולל דוגמאות למבנה ה-Body ב-JSON ומקרי קצה לבדיקה (כמו קורס שלא קיים או שדות חסרים)."

תשובה
> *"בדיקה מומלצת ל-POST `/courses`:
> * **Header:** `Content-Type: application/json`
> * **Body:**
> 
> 
> ```json
> {
>   "title": "Node.js Basics",
>   "duration": "4 weeks",
>   "level": "Beginner"
> }
> 
> ```
> 
> 
> * **מקרה קצה:** שלח בקשה ללא השדה `title` וודא שחוזר סטטוס `400 Bad Request` עם הודעה מתאימה."*
> 
> 

---

### 3. פרומפטים לפתרון תקלות והרצה (Debugging & Environment)

#### **פרומפט 3:**

> "בזמן הרצת הפקודה `node app.js` אני מקבל שגיאה `MODULE_NOT_FOUND`. הקובץ נמצא בתוך תיקיית `src/`. מה הסיבה לשגיאה וכיצד פותרים אותה?"

תשובה
> *"השגיאה נובעת מכך ש-Node.js מחפש את הקובץ בשורש הפרויקט בעוד שהוא ממוקם בתוך `src/`. כדי לפתור זאת יש להריץ `node src/app.js` או להיכנס לתיקייה בעזרת `cd src` ולאחר מכן להריץ `node app.js`."*

---

### 4. פרומפטים להוספת תכונות ולוגיקה עסקית (Features)

#### **פרומפט 4:**

> "אני רוצה להוסיף לוגיקה בשכבת ה-Service המונעת מרישום כפול של תלמיד לאותו קורס. כיצד מומלץ לממש זאת בתוך `enrollmentService.js`?"

תשובה
> *"ניתן להשתמש בשיטת `includes` על מערך ה-`enrolledCourses` של התלמיד לפני הוספת הקורס. אם ה-ID כבר קיים, נזרוק שגיאה:
> ```javascript
> if (student.enrolledCourses.includes(courseId)) {
>     throw new Error("התלמיד כבר רשום לקורס זה");
> }
> ```"*
> 
> ```
> 
>